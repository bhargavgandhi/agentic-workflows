# CODEBASE.md — {project-name}

> Generated by `codebase-mapper` skill. Update on major structural changes.  
> Last mapped: {date}

## App Overview

{1–3 sentence description of what this app does and its primary audience. Pull from README or package.json description.}

**Stack**: {language} / {framework} / {database} / {hosting}

---

## Folder Map

| Directory | Layer | Purpose |
|---|---|---|
| `{path}/` | {layer} | {one-paragraph purpose} |

---

## Layer Diagram

```
{project-root}/
├── {entry-point dir}/     ← Entry points (routes, mains, CLI)
├── {business-logic dir}/  ← Business logic (services, use-cases)
├── {data-access dir}/     ← Data access (repositories, models, migrations)
├── {ui dir}/              ← UI / Presentation (components, views)
├── {shared dir}/          ← Shared utilities (helpers, lib)
├── {config dir}/          ← Config / Infra (env, docker, CI)
└── {test dir}/            ← Tests
```

---

## Key Files

| File | Responsibility |
|---|---|
| `{path/to/file}` | {one-line description} |

---

## Where to Make Common Changes

| Task | Where to look |
|---|---|
| Add a new API endpoint | `{routes or controllers path}` |
| Add a new database model | `{models or schema path}` |
| Add a UI component | `{components path}` |
| Change business logic for {feature} | `{services path}` |
| Add or modify an env var | `.env.example` + `{config path}` |
| Update CI pipeline | `.github/workflows/` |

---

## Related Artifacts

- [CONFIG-MAP.md](.codebase-intel/CONFIG-MAP.md) — ENV vars and feature flags _(link only if `env-scanner` has been run and CONFIG-MAP.md exists)_
