# CONFIG-MAP — {project-name}

> Generated by `env-scanner` skill. Update whenever new env vars are added.  
> Last scanned: {date}

## Summary

| Total Variables | Credentials | Feature Flags | Config | URLs | Unknown |
|---|---|---|---|---|---|
| {n} | {n} | {n} | {n} | {n} | {n} |

## Variable Reference

| Variable | Type | Required | Default | Used In |
|---|---|---|---|---|
| `{VAR_NAME}` | credential / config / feature-flag / url | yes / no / conditional | `{value}` or `—` | `{file}:{line}` |

## Cross-Check: Undocumented Variables

Variables found in source code but **absent from `.env.example`**:

- `{VAR_NAME}` — found in `{file}:{line}` — add to `.env.example`

_(None if blank)_

## Cross-Check: Stale Variables

Variables present in `.env.example` but **not referenced in source code**:

- `{VAR_NAME}` — may be unused or used in a removed code path

_(None if blank)_

## Feature Flags

Variables that control feature rollout (type = `feature-flag`):

| Flag | Default | Behaviour When True |
|---|---|---|
| `{FEATURE_FLAG_NAME}` | `false` | {description of what enabling this does — or "Behaviour not determinable from env scan alone; read {file}:{line}" if logic is non-trivial} |
